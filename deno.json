{
  "name": "@funcwc/core",
  "version": "1.0.0",
  "description": "Ultra-lightweight, type-safe SSR components with DOM-native state management and hybrid reactivity",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/username/funcwc.git"
  },
  "keywords": [
    "ssr",
    "components",
    "typescript",
    "deno",
    "htmx",
    "dom-native",
    "reactivity",
    "web-components",
    "functional",
    "zero-runtime"
  ],
  "exports": {
    ".": "./index.ts"
  },

  "tasks": {
    "check": "deno check index.ts lib/**/*.ts examples/**/*.ts examples/**/*.tsx",
    "test": "deno test",
    "fmt": "deno fmt",
    "fmt:check": "deno fmt --check",
    "lint": "deno lint",
    "coverage": "deno test --coverage=coverage && deno coverage coverage --lcov > coverage.lcov",
    "serve": "cd examples && deno run --allow-net --allow-read --allow-env server.ts",
    "start": "deno task check && deno task serve",
    "docs": "deno doc index.ts"
  },

  "compilerOptions": {
    "strict": true,
    "lib": ["ES2021", "DOM", "deno.ns"],
    "jsx": "react",
    "jsxFactory": "h"
  },

  "imports": {
    "jsx": "./lib/jsx-runtime.ts"
  }
}
